// lib/core/utils/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // This is the single source of truth for your app's main blue color.
  static const Color primaryBlue = Color(0xFF007AFF);
}
